import React, { useEffect } from 'react'
import { View } from 'react-native'

export const biblioteca = ({router}) => {
    const {data} = router.params
    useEffect(() => {
    })
  return (
    <View>

    </View>
  )
}
